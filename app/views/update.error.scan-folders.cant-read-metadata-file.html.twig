<h2>Kan <code>portfolio.yml</code> niet inlezen</h2>

<p>
In de map met portfolio-groepen staat een map met de naam <strong>{{ failure.path }}</strong>. Het <code>portfolio.yml</code> bestand wat daar in staat is onleesbaar.
<p>

<section class="exception">
De daadwerkelijke (interne) foutmelding is:
<p class="exception">
{{ failure.exception_message }}
</p>
</section>
